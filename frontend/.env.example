# ── Frontend environment (copy to frontend/.env before building) ──
# In production this points to your live domain (same host, /api proxied by Nginx)
VITE_BACKEND_URL=https://myepicmoments.com
VITE_RAZORPAY_KEY_ID=rzp_live_xxxxxxxx
